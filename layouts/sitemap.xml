<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">

  {{ range .Site.RegularPages }}
    {{ if not .Params.sitemap.disable }}
      <url>
        <loc>{{ .Permalink }}</loc>
        {{ with .Lastmod.Format "2006-01-02T15:04:05-07:00" }}
          <lastmod>{{ . }}</lastmod>
        {{ end }}
        <changefreq>{{ cond (gt .Page.Date.Unix now.Unix) "monthly" (.Sitemap.ChangeFreq | default "weekly") }}</changefreq>
        <priority>{{ .Sitemap.Priority | default 0.5 }}</priority>
      </url>
    {{ end }}
  {{ end }}

</urlset>
